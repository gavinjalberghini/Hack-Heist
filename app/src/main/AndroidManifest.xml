<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.vcu.mymail.alberghinig.hackheist">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon,android:theme">
        <activity android:name=".Completion"></activity>
        <activity android:name=".LevelContents" />
        <activity android:name=".UnityLauncher" />
        <activity android:name=".SignUp" />
        <activity android:name=".Review" />
        <activity android:name=".Help" />
        <activity android:name=".DevInformation" />
        <activity android:name=".Leaderboard" />
        <activity android:name=".Forgot" />
        <activity android:name=".SetSecurityQuestion" />
        <activity android:name=".UserSettings" />
        <activity android:name=".Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".Login" />
    </application>

</manifest>